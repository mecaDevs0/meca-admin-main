import { TogglePasswordDisplayDirective } from './toggle-password-display.directive';

describe('TogglePasswordDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new TogglePasswordDisplayDirective();
    expect(directive).toBeTruthy();
  });
});

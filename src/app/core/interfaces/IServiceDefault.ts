export interface IServiceDefault {
  name: string;
  quickService: boolean;
  minTimeScheduling: number;
  description: string;
  photo: string;
  dataBlocked: number;
  disabled: number;
  created: number;
  id: string;
}

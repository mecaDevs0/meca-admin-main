import { GenericEnumPipe } from './generic-enum.pipe';

describe('GenericEnumPipe', () => {
  it('create an instance', () => {
    const pipe = new GenericEnumPipe();
    expect(pipe).toBeTruthy();
  });
});
